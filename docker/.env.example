# 后续上传镜像后替换为 ghcr.io/<账号>/detect-box-sim:v0.1.0。
DETECT_BOX_SIM_IMAGE=detect-box-sim:local

# 必须与仿真相机发布节点和 /detect 调用方一致。
ROS_DOMAIN_ID=0
ROS_LOCALHOST_ONLY=0

# 默认保持 ROS 2 Humble 的 Fast DDS；容器侧强制使用 UDP，避免跨容器共享内存传输问题。
RMW_IMPLEMENTATION=rmw_fastrtps_cpp
FASTDDS_BUILTIN_TRANSPORTS=UDPv4
